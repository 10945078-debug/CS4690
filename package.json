{
  "name": "node-starter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "npm run build --clean --verbose && node dist/app.js",
    "build": "tsc --build --verbose && copyfiles -u 0 db.json dist/",
    "prestart": "npm run build"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "copyfiles": "^2.4.1",
    "json-server": "^0.17.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/react-dom": "^19.2.3",
    "mongodb": "^7.2.0",
    "mongoose": "^9.4.1"
  }
}
